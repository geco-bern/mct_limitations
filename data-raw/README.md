# Raw project inputs

This directory contains small, immutable inputs that are versioned with the
analysis. `grid/` contains the CDO target-grid definitions at the resolutions
used by the existing workflow. Large raw datasets remain outside the repository
under their established `~/data/...` paths and are never modified by analysis
scripts.

